% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/legacy-data-ingestion.R
\name{ingest_legacy_16_20}
\alias{ingest_legacy_16_20}
\title{Ingest Legacy Zooplankton Data (2016-2020)}
\usage{
ingest_legacy_16_20()
}
\value{
Invisible NULL. Outputs are uploaded to SharePoint as versioned CSV
and Parquet files (prefix: \code{legacy-16-20}) to the automation bucket.
}
\description{
Downloads, validates, and harmonizes the 2016-2020 legacy zooplankton dataset
from SharePoint. Taxa are matched against the WoRMS database and the output
is written in both Parquet and CSV formats to the automation bucket.
}
\details{
The function performs the following steps:
\enumerate{
\item Downloads sample ID metadata (\code{ids_16_20.csv}) and biological data
(\code{zoo_16_20.csv}) from the \code{legacy_data} SharePoint bucket.
\item Integrates manually curated unmatched taxa from
\code{unmatched_worms_16_20.xlsx} to correct known synonyms before WoRMS
validation.
\item Queries WoRMS via \code{worrms::wm_records_taxamatch()} for every unique
taxon; unmatched taxa are flagged with \code{match_type = "no_match"}.
\item Selects a single AphiaID per taxon (lowest, i.e. oldest classification).
\item Joins biological observations with WoRMS-validated taxonomy and sample
metadata.
\item Standardises life-stage codes and converts abundance to numeric.
\item Aggregates duplicate records by summing \code{Abundance}.
\item Uploads tidy data to the automation bucket in Parquet and CSV formats.
}
}
\note{
Unmatched taxa (no WoRMS hit) are reported to the console and excluded
from the final export. Curators should review them and add corrections to
\code{unmatched_worms_16_20.xlsx} before re-running.
}
\examples{
\dontrun{
ingest_legacy_16_20()
}
}
\keyword{ingestion}
\keyword{legacy}
