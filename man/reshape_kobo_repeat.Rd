% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kobo-processing.R
\name{reshape_kobo_repeat}
\alias{reshape_kobo_repeat}
\title{Prepare repeat answers from Kobo survey forms}
\usage{
reshape_kobo_repeat(data, group_name, id_col = "submission_id")
}
\arguments{
\item{data}{Downloaded Kobo survey data frame.}

\item{group_name}{Prefix of the repeat group to unfold (e.g. \code{"group_taxa"}).}

\item{id_col}{Column used to order results (default: \code{submission_id}).}
}
\value{
Data frame with one row per submission and repeat entry.
}
\description{
Takes the repeating sample answers in MC survey exports (as used in
\code{preprocess_surveys()}) and lays them out so each repeat sits on its own
line, making the survey easier to read and join with other info.
}
\details{
Technical: pivots columns starting with the repeat group prefix,
splits out the repeat number and field name, spreads them back to columns,
and orders the rows by the ID column and repeat number.
}
