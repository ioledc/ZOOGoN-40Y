% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/legacy-data-ingestion.R
\name{ingest_legacy_84_15}
\alias{ingest_legacy_84_15}
\title{Ingest Legacy Zooplankton Data (1984-2015)}
\usage{
ingest_legacy_84_15()
}
\value{
Invisible NULL. Outputs are uploaded to SharePoint as
\verb{McZoo_84-15.parquet} and \verb{McZoo_84-15.csv}.
}
\description{
Downloads, validates, and harmonizes the 1984-2015 legacy zooplankton dataset
from SharePoint. Taxa are matched against the WoRMS database and the output
is written in both Parquet and CSV formats to the hot storage bucket.
}
\details{
The function performs the following steps:
\enumerate{
\item Downloads sample ID metadata (\code{ids_84_15.csv}) and biological data
(\code{zoo_84_15.csv}) from the \code{legacy_data} SharePoint bucket.
\item Integrates manually curated unmatched taxa from
\code{unmatched_worms_84_15.xlsx} to correct known synonyms before WoRMS
validation.
\item Queries WoRMS via \code{worrms::wm_records_taxamatch()} for every unique
taxon; unmatched taxa are flagged with \code{match_type = "no_match"}.
\item Selects a single AphiaID per taxon (lowest, i.e. oldest classification).
\item Joins biological observations with WoRMS-validated taxonomy and sample
metadata, pivoting date columns to long format.
\item Standardises life-stage codes.
\item Aggregates duplicate records by summing \code{Abundance}.
\item Uploads tidy data to the hot storage bucket in Parquet and CSV formats.
}
}
\note{
Unmatched taxa (no WoRMS hit) are reported to the console and excluded
from the final export. Curators should review them and add corrections to
\code{unmatched_worms_84_15.xlsx} before re-running.
}
\examples{
\dontrun{
ingest_legacy_84_15()
}
}
\keyword{ingestion}
\keyword{legacy}
